# Rule: Authentication and Authorization

## Standard
Protect non-public endpoints with authentication and authorization checks appropriate to user role and operation sensitivity.

## Scope
- Applies to all backend API endpoints except explicit health/public metadata endpoints.
- Applies to future endpoints by default.

## Rationale
Access control prevents unauthorized data access and reduces risk of misuse.
